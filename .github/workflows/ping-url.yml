git commit -m "Add GitHub Actions workflow"name: Ping URL every 5 minutes

on:
  push:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send GET request
        run: curl -s https://puch-ai-mcp-synapse07.onrender.com/mcp
